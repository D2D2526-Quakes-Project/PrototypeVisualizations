{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Building Index",
  "type": "object",
  "properties": {
    "size": { "type": "number" },
    "buildings": {
      "type": "array",
      "items": {
        "oneOf": [{ "$ref": "#/$defs/BinaryBuilding" }]
      }
    }
  },
  "required": ["size", "buildings"],
  "$defs": {
    "BinaryBuilding": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "folder": { "type": "string" },
        "size": { "type": "number" },
        "building_data": {
          "type": "string",
          "description": "Path to building data file (relative to /data/{folder}/) or full URL (http/https)"
        },
        "beamData": {
          "type": "string",
          "description": "Path to beam connectivity file (relative to /data/{folder}/) or full URL (http/https)"
        },
        "building_data_size": { "type": "number" },
        "simulations": {
          "type": "array",
          "items": { "$ref": "#/$defs/BinarySimulation" }
        }
      },
      "required": ["name", "folder", "size", "building_data", "building_data_size", "simulations"]
    },
    "BinarySimulation": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "folder": { "type": "string" },
        "size": { "type": "number" },
        "displacementLin": {
          "type": "string",
          "description": "Path to displacement linear file (relative to /data/{folder}/{simulation.folder}/) or full URL (http/https)"
        },
        "displacementRot": {
          "type": "string",
          "description": "Path to displacement rotation file (relative to /data/{folder}/{simulation.folder}/) or full URL (http/https)"
        },
        "velocityLin": {
          "type": "string",
          "description": "Path to velocity linear file (relative to /data/{folder}/{simulation.folder}/) or full URL (http/https)"
        },
        "velocityRot": {
          "type": "string",
          "description": "Path to velocity rotation file (relative to /data/{folder}/{simulation.folder}/) or full URL (http/https)"
        },
        "accelerationLin": {
          "type": "string",
          "description": "Path to acceleration linear file (relative to /data/{folder}/{simulation.folder}/) or full URL (http/https)"
        },
        "accelerationRot": {
          "type": "string",
          "description": "Path to acceleration rotation file (relative to /data/{folder}/{simulation.folder}/) or full URL (http/https)"
        },
        "groundMotion": {
          "type": "string",
          "description": "Path to ground motion file (relative to /data/{folder}/{simulation.folder}/) or full URL (http/https)"
        },
        "hingeData": {
          "type": "string",
          "description": "Path to hinge data file (relative to /data/{folder}/{simulation.folder}/) or full URL (http/https)"
        },
        "shearData": {
          "type": "string",
          "description": "Path to shear data file (relative to /data/{folder}/{simulation.folder}/) or full URL (http/https)"
        },
        "brbData": {
          "type": "string",
          "description": "Path to BRB data file (relative to /data/{folder}/{simulation.folder}/) or full URL (http/https)"
        }
      },
      "required": ["name", "folder", "size", "displacementLin", "groundMotion"]
    }
  }
}
